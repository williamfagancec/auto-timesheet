# Test Environment Configuration
# This file is used by Vitest for running integration tests
# DO NOT use production database credentials here!

# Test Database
# Uses a separate database to isolate test data from development/production
DATABASE_URL="postgresql://user:password@localhost:5432/timetracker_test?connection_limit=20&pool_timeout=10"

# Redis (can use same instance with different prefix, or separate test instance)
REDIS_URL="redis://localhost:6379/1"

# Authentication (test values - can be dummy for most tests)
SESSION_SECRET="test-session-secret-for-integration-tests-64-char-hex-here"
BETTER_AUTH_SECRET="test-session-secret-for-integration-tests-64-char-hex-here"
API_URL="http://localhost:3001"
GOOGLE_CLIENT_ID="test-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="GOCSPX-test-google-client-secret-for-tests"
GOOGLE_REDIRECT_URI="http://localhost:3001/auth/google/callback"

# Encryption (test key - NOT for production use)
ENCRYPTION_KEY="test-encryption-key-for-integration-tests-64-char-hex"

# API Configuration
PORT=3001
HOST="0.0.0.0"
NODE_ENV="test"

# Frontend URL (for CORS)
FRONTEND_URL="http://localhost:3000"
